#include <stdio.h>
/**
 * main - entry point
 * prints program name
 * @argc: argument count
 * @argv: argument array
 * Return: zero
*/
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[0]);
	return (argc);
}
